# DocDD 開発ルール

このプロジェクトはドキュメント駆動開発（DocDD）を採用しています。
実装はすべて仕様（exec-plan の AC）に基づいて行います。

## 仕様ファースト原則（必須）

**実装の指示を受けたとき、コードを書く前に必ず以下を確認する。**

### 確認チェックと応答

| 状態 | Claudeの応答 |
|------|-------------|
| `exec-plans/active/` に実行計画なし | `/create-exec-plan` を提案し、実装しない |
| exec-plan に `AC-001〜` の定義なし | AC の追加を求め、実装しない |
| AC はあるが指示に AC 番号なし | 「どの AC を実装しますか？」と確認し、実装しない |
| AC 番号が指定されている | 実装を開始する |

### 応答例

**exec-plan なしの場合:**
```
仕様（exec-plan）が定義されていません。
先に /create-exec-plan を実行して受け入れ基準（AC）を定義しましょう。
```

**AC が指定されていない場合:**
```
どの受け入れ基準（AC）を実装しますか？
現在の exec-plan: exec-plans/active/YYYY-MM-xxx.md
  - AC-001: ...
  - AC-002: ...
```

## 例外処理（仕様なしで進める場合）

ユーザーが「仕様なしで進めることを確認します」などの明示的な確認を与えた場合のみ：

1. `exec-plans/.spec-override` を作成する（理由とタイムスタンプを記録）
2. 実装を進める
3. 実装完了後に `exec-plans/.spec-override` を削除する

## 自律実装ループ（autonomous exec loop）

AC が凍結された範囲内の **実行**（テストを先に赤で置く→実装→テスト→修正→次の AC）は、`/run-exec-plan` スキルで
人間の確認を挟まず自走させてよい。一方、**何を作るか・仕様をどう変えるか**といった統治判断は
人間に残す。自走させるのは「実行」だけで、「決定」は渡さない。

### 自動化してよい範囲 / 残す範囲

| 層 | 例 | 担い手 |
|----|----|--------|
| 内側ループ（自動化） | AC 本文からのテスト起草と赤の観測、AC の実装、テスト実行、実装バグによる失敗の修正、再実行、次の AC への前進 | `/run-exec-plan`（確認なし） |
| 外側ゲート（自動化しない） | AC の定義、**AC が述べていない期待結果の決定**、凍結後のテスト期待値の変更、仕様変更、`/promote-spec`、PR 作成/push | 人間（停止して確認） |

テストを書くことが内側ループ側にあるのは「転記」の意味においてのみ。given/when/then は人が凍結した
AC 本文に既にあり（readiness の R2 が保証する）、driver はそれを実行可能な形に写すだけ。
AC が述べていない期待結果を **決める** 必要が生じた時点で外側ゲートであり、停止する。

### 停止条件（これ以外は継続する）

`/run-exec-plan` は次の場合「のみ」停止して人間に委ねる。これ以外の理由（「十分進んだ気がする」等）で
途中停止してはならない（＝context anxiety の回避）。

| ID | 条件 |
|----|------|
| (a) | AC が欠落・曖昧・実装に不十分（＝何を作るかの判断が必要）。**ループ開始前**の AC readiness ゲートで検出し、AC 本文からテストを起草できない場合（Step 0c / 2a）も同じ条件。ループ中は backstop として働く |
| (b) | 有限回（既定 3 回）の自己修正後もテストが赤。red-first のテストが**妥当な赤**に到達しない場合も同じ |
| (c) | テストの**期待値**変更が必要（テスト変更は仕様変更に紐づける＝INV-T01／赤の観測で凍結した期待値の変更＝INV-T02） |
| (d) | 不可逆/外向き操作が次に来る（PR 作成/push、`/promote-spec`、タグ削除） |
| (e) | スコープ拡張なしに解消できない INV 違反 |

### AC readiness（自走前の測定可能性ゲート）

痩せた AC には操舵できない。AC が**テストで合否判定できる形になっているか**の検査は、
`.claude/skills/create-exec-plan/ac-readiness.md` を**単一ソース**として次の4地点で同じ基準を適用する。
基準は同一で、違うのは「その場に AC を書き直せる人がいるか」だけ。

| 地点 | タイミング | NOT READY のときの措置 |
|------|-----------|----------------------|
| `/create-exec-plan`（Q3c） | プラン確定前 | 人と対話して AC を書き直す。書き直すまで確定しない |
| `/start-feature`（Step 1b） | 実装開始前（手動） | 人に提示して判断を仰ぐ。決定を Progress Log に記録 |
| `/run-exec-plan`（Step 0b） | ループ開始前 | **ループを開始せず HALT**（停止条件 (a)） |
| `/doc-review`（§2） | 任意レビュー | 助言として報告のみ（ファイルは変更しない） |

AC の書き直しは「何を作るか」の決定＝外側ゲートであり、スキルが代行してはならない。人が同席する
地点では対話で解決し、自走では止めるしかない。これは地点間の重大度の不一致ではなく、同じ原則の帰結。

### red-first（測定を実装から独立させる規約）

実装と同時に書かれたテストは「AC が満たされたか」ではなく「コードがどう動くか」を写す。
一度も赤になっていないテストは何も測っていない。したがって AC ごとに、**実装が存在しない状態で
AC 本文からテストを起草し、赤を観測してから実装する**。手順・妥当な赤と無効な赤の判別・記録書式・
凍結後の変更規則は `.claude/skills/run-tests/red-first.md` を**単一ソース**とする（＝INV-T02）。

| 地点 | タイミング | red-first できないときの措置 |
|------|-----------|---------------------------|
| `/run-exec-plan`（Step 0c） | ループ開始前・`[E2E]` AC ごと | 転記できなければ **HALT**（停止条件 (a)） |
| `/run-exec-plan`（Step 2a） | 各 AC の実装前 | 転記できなければ HALT (a)。無効な赤は有限回まで修復し、なお駄目なら HALT (b) |
| `/start-feature`（実装順ガイド） | 手動実装パス | 人に提示して判断を仰ぐ |
| `/pre-pr`・`/complete-exec-plan` | 実装後 | 証跡（赤の記録が done より前にあるか）を **⚠️ 報告のみ**。ブロックしない |
| `/docode-review` | 任意レビュー | テストが実装の写しになっていないかを独立エージェントが判定（助言） |

赤の観測は Decision Log という人が書くテキストに残るため、事後のゲートで**ブロックにはしない**
（記録漏れと規約違反を機械的に区別できず、ブロックすると証跡を後から書く動機を生むため）。
実効的なブロックは従来どおり AC カバレッジ・E2E カバレッジが担う。

### 検証スキルの呼び出しポリシー

`/run-exec-plan` は検証スキルを AC ごとに自動で回す。ただし呼び出し方式はスキルの副作用の有無で分ける。

| スキル | `disable-model-invocation` | 呼び出し方式 | 理由 |
|--------|:--:|--------------|------|
| `run-tests` | **false** | Skill ツールで起動（単一ソース） | テスト実行は read-only・非破壊。driver が AC ごとに確実に呼ぶ必要があり、モデル起動可にして Skill ツールで呼ぶ方がインライン複製のドリフトを避けられる |
| `check-invariants` / `check-doc-freshness` | **true** | 手順をインライン実行（Skill ツールは使わない） | コード/ドキュメントを書き換える副作用があるため、文脈任せの自動起動（ambient invocation）は統治ゲートを崩す。driver や pre-pr が手順を辿る形でのみ実行する |

`true` のスキルは「モデルが文脈次第で勝手に起動するのを防ぎつつ、driver や pre-pr が手順を辿る形での実行は可能」という仕組みをそのまま使う。
人間判断スキル（`create-requirements` の対話、`promote-spec`、PR 作成）はゲートを維持する。

### 再開状態のファイル化規約

自走中の状態は **exec-plan ファイルだけで再開可能** にする（会話履歴に依存しない）。

- AC のチェックボックス（`- [x]`）が「何が完了したか」の正とする。
- AC の**実装前**に `## Decision Log` へ「AC-NNN red-first: `<テスト名>` を AC 本文から起草し赤を観測
  （expected/actual）。以降この期待値は凍結」を追記する（免除の場合はその理由）。
- AC 完了ごとに `## Decision Log` へ「AC-NNN done. 実装内容＋主要ファイル。テスト緑（n 件）」を追記する。
  同じ AC の red-first 記録が done 記録より前にあることが、テストが実装の写しでない証跡になる。
- 停止（HALT）時は「HALT at AC-NNN（停止条件 <id>）。ブロッカー。人間が判断すべき事項」を追記する。

新しいセッションは、上記チェックボックスと Decision Log のみで作業を再開できなければならない。

## ドキュメント内の図・ダイアグラム規則

ドキュメントに図を記載する際は以下のルールに従う。

| 状況 | ルール |
|------|--------|
| フロー・シーケンス・クラス図など | **Mermaid を優先して使用する** |
| Mermaid で表現できない図（UI スケッチ・レイアウト等） | AA（アスキーアート）を使用し、**直後に必ず図の説明文を添える** |

**AA に必ず添える説明文の例:**

```
┌──────────┬──────────┐
│ ファイル名 │ タグ     │
└──────────┴──────────┘
```

上図はファイル一覧画面のレイアウト。左列にファイル名、右列に付与済みタグを表示する。
行を選択するとタグ編集パネルが右側にスライドインする。

## テストの作成順と変更の禁止

テストは AC より後・実装より**先**に書く（red-first ＝ INV-T02）。AC 本文だけを見て起草し、
実行して妥当な赤を観測し、その観測を Decision Log に記録した時点で期待値を凍結する。
手順は `.claude/skills/run-tests/red-first.md` を参照。

テストを修正する場合は対応する AC-ID を必ず確認すること。
実装の挙動に合わせたテスト修正は禁止（INV-T01）。変更理由を exec-plan の Decision Log に記録する。

## 仕様バージョンの管理（ブランチ運用）

仕様の更新は「現バージョンの修正」と「次バージョンの変更」を分離して管理する。
これにより、実装着手中に対象とする仕様が動くことを防ぎ、追従の判断を制御された地点（昇格）に集約する。

| 対象 | 置き場所 | 説明 |
|------|----------|------|
| 現在の開発ターゲット仕様 ＋ コード | `main` | 実装が *今* 追従すべき凍結された目標。`exec-plans/active/` は常にこの仕様に対応する |
| 次バージョンの仕様変更（docs のみ） | `spec/<label>` ブランチ | 例 `spec/sprint-12`。スプリント単位で作成。コードや exec-plan は含めない |

### 変更のルーティング（どちらに書くか）

| 変更種別 | 行き先 | 扱い |
|----------|--------|------|
| **現バージョンの不備修正**（仕様の誤り・矛盾・曖昧さの是正。スコープを広げない） | `main` に直接 | in-flight 作業に伝播させる（意図どおり）。既に実装済みの AC が影響を受ける場合は「stale」として reconcile 対象にする |
| **次バージョンの変更**（新機能・スコープ拡張・方針転換） | `spec/<label>` | 昇格まで現ターゲットを揺らさない |

判断基準は「これは『今作っている物が間違っている』のか、『次に作りたい物』なのか」。
ルーティングの判断とその理由は、対応する exec-plan の Decision Log に必ず記録する。

### 現バージョン修正による stale の扱い

`main` への不備修正が **既に実装済み** の AC の仕様を変える場合、その実装は新しい仕様に対して stale になる。
「なぜ stale にしたか」（履歴）と「reconcile すべき未処理作業」を別チャネルで管理する。

| 対象 AC の状態 | 対応 |
|----------------|------|
| **実装中**（`exec-plans/active/` にある） | そのプランの Decision Log に変更理由を記録する。作業は既に開いているため追加のプランは不要 |
| **完了済み**（`exec-plans/completed/` にある） | ① reconcile プランに同じ AC-ID を再オープンする（`active/` 内の最新の `*-reconcile.md` に追記、なければ当月の `exec-plans/active/YYYY-MM-reconcile.md` を作成）。② その **reconcile プラン側** の Decision Log に「AC-XXX は `<commit>` の不備修正で stale」と記録する（完了済みプランは archive 済みで参照されないため、そこには書かない） |

- 未処理の reconcile は必ず `exec-plans/active/` に置く（spec-gate・pre-pr・gc が走査する唯一の場所）。
- 専用の「Spec Drift Log」節は作らない（走査されず archive されて陳腐化するため）。
- reconcile プランは無限に追記せず、サイクルごとに閉じて新規を起こす（全 AC が `- [x]` にならず complete できなくなるのを防ぐ）。命名は in-version が `YYYY-MM-reconcile.md`、昇格起点（`/promote-spec`）が `YYYY-MM-reconcile-<label>.md`。両者は `active/` に併存し、扱い（同 AC-ID 再オープン＋Decision Log 記録）は同一。
- 当面 reconcile しないと判断した場合は、その active プランの Decision Log に `reconcile: deferred`（または `reconcile: n/a`）と記録する。これにより `/gc` backstop の再フラグを抑止する。
- 起票漏れは `/gc` の spec-drift backstop が週次で検出する（結果は確認候補であり確定ではない）。

### 昇格（promotion）

次バージョン仕様を現ターゲットに取り込む操作は **`/promote-spec`** で行う。
昇格は開発ターゲットを変える判断のため、**マージするか・いつするかは人が決める**（スキルは差分・影響解析と後処理を支援するのみ）。
各ターゲットのスナップショットは、それぞれ固有の `spec-target-<label>` タグで保存される（ラベルはターゲットごとに異なり、同名タグを使い回さない）。昇格後の新版には新しいラベルのタグを付け、昇格前の現行版は前回の昇格で付いたタグで既に保存済み（初回昇格のみ baseline を seed）。これにより旧バージョンはいつでも復元・差分比較できる。タグは共有リモートへ push して初めて全員が復元できる点に注意。

**`spec-target-*` タグは昇格（＝開発ターゲットの変更）ごとに作る。個人や機能ごとには作らない。**
`<label>` は共有のバージョン/昇格ラベルであり、個人名・機能名は使わない。開発者の機能実装は通常の PR で現ターゲット（`main`）にマージするだけで、タグは付かない（タグが付くのは `/promote-spec` の中だけ）。したがってタグ数は「昇格回数 ＝ 開発ターゲットを切り替えた回数」に等しく、開発者数や機能数には比例しない。
復元の正は昇格の `--no-ff` マージコミット（`main` 上に必ず残る）であり、`spec-target-*` タグは名前付き diff のための便宜にすぎない。古いタグは履歴を失わずに剪定できる（`git tag -d` ＋ `git push origin --delete`）。

## スキル一覧

| スキル | 目的 | タイミング |
|--------|------|-----------|
| `/create-requirements` | User Story・**ゴール像**（完成時にできること／主要ユーザージャーニー／非ゴール）・制約・AC を定義する | 実装前・create-spec の前 |
| `/doc-review` | 独立エージェントによるドキュメントレビュー | create-requirements / create-spec 後・create-exec-plan の前（任意） |
| `/create-spec` | 承認済み要件からアプリ仕様（docs/02_spec/：何をするか）と **E2E シナリオ**（`E2E-001 → AC-001, AC-003` の横断 traceability）を草案として起草する | create-requirements 後・create-exec-plan の前 |
| `/create-exec-plan` | AC（受け入れ基準）と **`[E2E]` AC**（documentation-only プランは置かず `E2E: n/a` を記録）を定義し、**AC readiness**（測定可能性）を検査する | 実装前（必須） |
| `/start-feature` | 実装開始前の準備チェック（AC readiness の再検査を含む） | create-exec-plan 後 |
| `/run-exec-plan` | ループ前に AC readiness を検査し、`[E2E]` テストを赤で配置。AC を1つずつ自走実装（テストを先に赤で置く→実装→テスト→修正→次）。停止条件のみで人間に委譲 | start-feature 後・自走させたいとき（opt-in） |
| `/run-tests` | テスト実行と仕様照合（red-first 実行では妥当な赤／無効な赤を判別） | 実装前（赤の確認）・実装中・完了時 |
| `/check-doc-freshness` | コードとドキュメントの乖離確認 | コード変更後 |
| `/check-invariants` | コードの INV 違反確認 | コード変更後 |
| `/check-doc-invariants` | ドキュメントの構造ルール違反確認 | ドキュメント変更後 |
| `/promote-spec` | 次バージョン仕様（`spec/<label>`）を現ターゲットへ昇格 | スプリント境界・次バージョン取り込み時 |
| `/docode-review` | 独立エージェントによるコードレビュー | 実装完了後・`/pre-pr` の前 |
| `/pre-pr` | PR 前の総合チェック | PR 作成前（必須） |
| `/complete-exec-plan` | exec-plan を完了状態に移動 | PR マージ後 |
| `/gc` | 全体的なドリフト解消 | 週次 |
